package com.dominare.api.model.apartamento.entrega;

import org.springframework.data.jpa.repository.JpaRepository;

public interface EntregasRepository extends JpaRepository <EntregaModel, Long> {
    
}
